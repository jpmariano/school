/** @type {import('next').NextConfig} */
const path = require('path')
const nextConfig = {

}

/*
module.exports = {
    reactStrictMode: true,
    webpack: (config) => {
      config.resolve = {
        ...config.resolve,
        fallback: {
          fs: false,
        },
      };
      return config;
    },
} */

module.exports = {

} 
